# Force LF in the working tree on every platform. These files are deployed to
# Home Assistant OS (Linux); a CRLF Dockerfile breaks the Alpine build, and a
# CRLF shell/YAML file can fail in ways that are hard to diagnose.
* text=auto eol=lf
